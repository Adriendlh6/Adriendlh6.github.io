/* Mon entreprise page module */
(function(){
  function qs(sel, root=document){ return root.querySelector(sel); }
  function esc(s=''){ return String(s ?? '').replace(/[&<>\"]/g,m=>({ '&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;' }[m])); }
  function num(v){ if(v===null||v===undefined) return 0; if(typeof v==='number') return Number.isFinite(v)?v:0; const p=Number(String(v).replace(/\s+/g,'').replace(',','.').replace(/[^0-9.\-]/g,'')); return Number.isFinite(p)?p:0; }
  function euro(v){ return new Intl.NumberFormat('fr-FR',{style:'currency',currency:'EUR'}).format(Number(v||0)); }
  function pct(v){ return Number.isFinite(Number(v)) ? `${new Intl.NumberFormat('fr-FR',{maximumFractionDigits:1}).format(Number(v))} %` : 'N/C'; }
  const STORE='entreprise'; const ID='mon-entreprise'; let company=null;
  function normalize(data={}){ return { id:ID, nomCommercial:data.nomCommercial||'', raisonSociale:data.raisonSociale||'', siret:data.siret||'', tvaIntracom:data.tvaIntracom||'', telephone:data.telephone||'', mail:data.mail||'', adresse:data.adresse||'', validiteDevisJours:data.validiteDevisJours===undefined||data.validiteDevisJours===''?30:num(data.validiteDevisJours), delaiPaiement:data.delaiPaiement||'', tvaDefault:data.tvaDefault===undefined||data.tvaDefault===''?5.5:num(data.tvaDefault), prefixeDevis:data.prefixeDevis||'DEV', mentionsDevis:data.mentionsDevis||'', caHT:data.caHT??'', achatsMatieresHT:data.achatsMatieresHT??'', fraisGeneraux:data.fraisGeneraux??'', masseSalarialeProduction:data.masseSalarialeProduction??'', heuresProductives:data.heuresProductives??'', baseRepartition:data.baseRepartition||'ca', note:data.note||'', updatedAt:data.updatedAt||new Date().toISOString() }; }
  function empty(){ return normalize({}); }
  function baseAmount(c){ const b=c.baseRepartition||'ca'; if(b==='matiere') return {label:'Achats matières HT', amount:num(c.achatsMatieresHT)}; if(b==='salaires') return {label:'Masse salariale production', amount:num(c.masseSalarialeProduction)}; return {label:'Chiffre d’affaires HT', amount:num(c.caHT)}; }
  function compute(c){ const frais=num(c.fraisGeneraux); const base=baseAmount(c); const rate=base.amount>0?(frais/base.amount)*100:0; const coefficient=base.amount>0?1+(frais/base.amount):0; const heures=num(c.heuresProductives); const salaires=num(c.masseSalarialeProduction); const coutHoraire=heures>0?(salaires+frais)/heures:0; return {frais,base,rate,coefficient,heures,salaires,coutHoraire}; }
  function fill(c){ const form=qs('#company-form'); if(!form) return; Object.entries(c).forEach(([k,v])=>{ if(form.elements[k]) form.elements[k].value=v??''; }); }
  function read(){ const form=qs('#company-form'); if(!form) return empty(); const fd=new FormData(form); const obj={...company}; ['nomCommercial','raisonSociale','siret','tvaIntracom','telephone','mail','adresse','validiteDevisJours','delaiPaiement','tvaDefault','prefixeDevis','mentionsDevis','caHT','achatsMatieresHT','fraisGeneraux','masseSalarialeProduction','heuresProductives','baseRepartition','note'].forEach(k=>obj[k]=String(fd.get(k)||'').trim()); return normalize(obj); }
  function renderCalc(){ const c=read(); const calc=compute(c); const coef=calc.coefficient?new Intl.NumberFormat('fr-FR',{minimumFractionDigits:2,maximumFractionDigits:2}).format(calc.coefficient):'N/C'; const rate=calc.base.amount?pct(calc.rate):'N/C'; const hour=calc.coutHoraire?`${euro(calc.coutHoraire)} / h`:'N/C'; const a=qs('#company-kpi-coef'),b=qs('#company-kpi-rate'),d=qs('#company-kpi-hour'); if(a)a.textContent=coef; if(b)b.textContent=rate; if(d)d.textContent=hour; const grid=qs('#company-calculation-grid'); if(!grid)return; grid.innerHTML=`<div><span>Base utilisée</span><strong>${esc(calc.base.label)}</strong><small>${calc.base.amount?euro(calc.base.amount):'Non renseignée'}</small></div><div><span>Frais généraux</span><strong>${calc.frais?euro(calc.frais):'N/C'}</strong><small>Charges indirectes annuelles.</small></div><div><span>Coefficient</span><strong>${coef}</strong><small>1 + frais généraux / base.</small></div><div><span>Coût horaire repère</span><strong>${hour}</strong><small>MO production + frais généraux / heures.</small></div>`; }
  async function save(){ company=read(); await AppDB.put(STORE, company); renderCalc(); const btn=qs('#save-company-top-btn'); if(btn){ const old=btn.textContent; btn.textContent='✓'; setTimeout(()=>btn.textContent=old,700); } }
  async function reset(){ if(!confirm('Réinitialiser les informations de Mon entreprise ?')) return; company=empty(); await AppDB.put(STORE, company); fill(company); renderCalc(); }
  async function render(){ let stored=null; try{ stored=await AppDB.get(STORE, ID); }catch(e){ console.error('[mon-entreprise]', e); } company=stored?normalize(stored):empty(); fill(company); renderCalc(); const form=qs('#company-form'); form?.addEventListener('input', renderCalc); form?.addEventListener('change', renderCalc); form?.addEventListener('submit', async e=>{ e.preventDefault(); await save(); }); qs('#save-company-top-btn')?.addEventListener('click', save); qs('#reset-company-btn')?.addEventListener('click', reset); }
  window.MonEntreprisePage={render};
})();
